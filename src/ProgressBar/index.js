import React from 'react';
import {style} from './index.scss';

const ProgressBar = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

ProgressBar.propTypes = {

};

export default ProgressBar;
